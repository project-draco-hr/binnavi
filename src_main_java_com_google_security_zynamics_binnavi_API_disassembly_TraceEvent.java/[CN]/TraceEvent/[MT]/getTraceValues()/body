{
  final ArrayList<TraceRegister> registers=new ArrayList<TraceRegister>();
  for (  final com.google.security.zynamics.binnavi.debug.models.trace.TraceRegister register : m_event.getRegisterValues()) {
    registers.add(new TraceRegister(register));
  }
  return registers;
}
