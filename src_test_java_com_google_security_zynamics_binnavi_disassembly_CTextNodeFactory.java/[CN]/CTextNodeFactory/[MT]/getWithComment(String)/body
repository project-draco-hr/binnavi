{
  final IComment comment=new CComment(null,CommonTestObjects.TEST_USER_1,null,commentText);
  return new CTextNode(1,0,0,0,0,Color.RED,false,true,new HashSet<CTag>(),Lists.newArrayList(comment),new MockSqlProvider());
}
