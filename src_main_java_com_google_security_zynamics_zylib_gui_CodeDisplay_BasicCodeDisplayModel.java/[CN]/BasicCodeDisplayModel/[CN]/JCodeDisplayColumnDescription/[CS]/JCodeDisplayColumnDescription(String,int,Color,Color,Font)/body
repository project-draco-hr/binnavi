{
  name=columnName;
  width=columnWidth;
  defaultFontColor=fontColor;
  defaultBackgroundColor=backgroundColor;
  defaultHeaderFont=headerFont;
  headerLine=new FormattedCharacterBuffer(CodeDisplay.padRight(name,width),defaultHeaderFont,defaultFontColor,defaultBackgroundColor);
}
