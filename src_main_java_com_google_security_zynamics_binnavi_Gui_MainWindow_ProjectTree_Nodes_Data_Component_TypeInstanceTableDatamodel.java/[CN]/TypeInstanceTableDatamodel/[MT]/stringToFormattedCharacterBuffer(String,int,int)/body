{
  return new FormattedCharacterBuffer(Strings.padEnd(string,columns[columnIndex].getWidth(),' '),STANDARD_FONT,columns[columnIndex].getDefaultFontColor(),getBackgroundColor(rowIndex,columnIndex));
}
