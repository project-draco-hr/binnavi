{
  final int typeId=provider.createTypeMember(module,containingType.getId(),memberType.getId(),memberName,Optional.of(memberOffset),Optional.<Integer>absent(),Optional.<Integer>absent());
  return TypeMember.createStructureMember(typeId,containingType,memberType,memberName,memberOffset);
}
