{
  return CollectionHelpers.map(sourceNodes,new ICollectionMapper<INaviViewNode,INaviViewNode>(){
    @Override public INaviViewNode map(    final INaviViewNode item){
      return nodeMap.get(item);
    }
  }
);
}
