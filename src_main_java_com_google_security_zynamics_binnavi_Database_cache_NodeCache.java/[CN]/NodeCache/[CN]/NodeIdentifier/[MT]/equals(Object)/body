{
  if (this == other) {
    return true;
  }
  if (other instanceof NodeIdentifier) {
    return Objects.equals(address,((NodeIdentifier)other).address) && Objects.equals(moduleId,((NodeIdentifier)other).moduleId);
  }
  return false;
}
