{
  final INaviModule module=getProvider().loadModules().get(0);
  module.load();
  PostgreSQLSettingsFunctions.writeSetting((AbstractSQLProvider)getProvider(),(CModule)module,"key",String.valueOf(1));
}
