{
  final CModuleViewGenerator generator=new CModuleViewGenerator(sql,module);
  return generator.generate(1,"Mock View","Mock View Description",ViewType.Native,GraphType.MIXED_GRAPH,new Date(),new Date(),1,1,new HashSet<CTag>(),new HashSet<CTag>(),false);
}
