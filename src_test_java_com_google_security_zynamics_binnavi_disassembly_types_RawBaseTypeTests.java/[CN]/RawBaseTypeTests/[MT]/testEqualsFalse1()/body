{
  final RawBaseType rawType0=new RawBaseType(ID,NAME + "narf",SIZE,POINTER_ID,true,BaseTypeCategory.ATOMIC);
  final RawBaseType rawType1=new RawBaseType(ID,NAME,SIZE,POINTER_ID,true,BaseTypeCategory.ATOMIC);
  assertFalse(rawType0.equals(rawType1));
}
