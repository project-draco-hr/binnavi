{
  if (!reply.success()) {
    CMessageBox.showError(activateWindow(),String.format("Could not read the registers of the target thread.\n\nReason: %s",DebuggerErrorCodes.codeToMessage(reply.getErrorCode())));
  }
}
