{
  if (!reply.success()) {
    CMessageBox.showError(activateWindow(),String.format("Could not do single-step operation.\n\nReason: %s",DebuggerErrorCodes.codeToMessage(reply.getErrorCode())));
  }
}
