{
  if (!reply.success()) {
    CMessageBox.showError(activateWindow(),String.format("Could not determine the memory layout of the target process.\n\nReason: %s",DebuggerErrorCodes.codeToMessage(reply.getErrorCode())));
  }
}
