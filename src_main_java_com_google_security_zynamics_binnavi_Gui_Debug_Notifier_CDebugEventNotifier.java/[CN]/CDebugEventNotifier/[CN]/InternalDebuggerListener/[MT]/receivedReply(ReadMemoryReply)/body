{
  if (!reply.success()) {
    NaviLogger.info(String.format("Could not read memory from the target process since the " + "memory map might have changed. Refreshing the memory map.\nError code was: %d",reply.getErrorCode()));
  }
}
