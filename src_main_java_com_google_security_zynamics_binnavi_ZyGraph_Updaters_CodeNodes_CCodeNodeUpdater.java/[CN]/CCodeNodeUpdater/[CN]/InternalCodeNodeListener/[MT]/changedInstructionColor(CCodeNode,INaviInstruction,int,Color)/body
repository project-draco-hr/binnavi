{
  if (color == null) {
    node.clearHighlighting(level,CCodeNodeHelpers.instructionToLine(CCodeNodeUpdater.this.codeNode,instruction));
  }
 else {
    node.setHighlighting(level,CCodeNodeHelpers.instructionToLine(CCodeNodeUpdater.this.codeNode,instruction),color);
  }
}
