{
  final List<NodeType> list=new ArrayList<NodeType>();
  for (  final ZyGraphNode<? extends NodeType> node : visibleNodes) {
    final NodeType rawNode=node.getRawNode();
    list.add(rawNode);
  }
  return list;
}
