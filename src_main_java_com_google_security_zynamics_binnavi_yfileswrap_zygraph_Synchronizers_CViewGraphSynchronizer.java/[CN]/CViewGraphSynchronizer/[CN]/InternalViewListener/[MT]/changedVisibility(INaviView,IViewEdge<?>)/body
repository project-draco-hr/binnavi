{
  m_edgeDrawingSynchronizer.updateEdgeDrawingState();
  final NaviEdge naviEdge=m_mappings.getEdge(edge);
  if (edge != null) {
    naviEdge.getRealizer().setDrawSloppyEdges(CEdgeDrawingFunctions.calcDrawSloppyEdges(m_graph));
  }
}
